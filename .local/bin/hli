#!/bin/bash

INPUT_FILE=$(ls -t $HOME/Downloads/EASYBANK_*.csv | head -n1)
OUTPUT_FILE="$HOME/Documents/easybank.csv"

iconv -f ISO8859-1 -t UTF8 $INPUT_FILE -o $OUTPUT_FILE
hledger import $OUTPUT_FILE
